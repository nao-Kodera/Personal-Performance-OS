# Personal Performance OS — 実装ガイドライン

このリポジトリで実装作業を行う際の指示。**設計書（`docs/01`〜`09`）が唯一の仕様源であり、本書はその読み方と実装時の振る舞いを定める。**仕様そのものは本書に書かない（二重化を避けるため）。

汎用の実装用プロンプト（スタック非依存の完全版）は `D:\Users\nao\workspace\資料置き場\開発用スクリプト\implementation-prompt.md` にある。本書はその**このプロジェクト版**である。

---

## 0. このプロダクトの目的（実装中に失われやすい）

> タスクを管理することが目的ではない。自分の状態・環境・行動・成果を観測し、高いパフォーマンスが出る条件を発見して再現することが目的である。

判定基準: 実装しようとしている機能が**「タスクを消化しやすくするため」ならスコープ外**、**「作業の条件と結果を観測するため」ならスコープ内**（[PRD §12](docs/01-product-requirements.md)）。

根幹の4点は [docs/README.md](docs/README.md#L36-L45) にある。特に **開始前の状態 / 作業中の事実 / 作業後の結果を分離する**（説明変数と目的変数の独立性）を壊す変更をしない。

---

## 1. 判断原則

1. **実装する仕様は、設計書に根拠があること。** 設計書に無い仕様を独断で追加しない。
2. **共通化・責務配置は、変更理由が同一であること。** 形が似ていることを理由に共通化しない。
3. **抽象化は、現に必要な責務・現に存在する重複に対してのみ行うこと。** 「将来のため」で層・オプション引数・設定項目を足さない（[技術設計 §0](docs/08-technical-design.md) の「必要になるまで入れない」）。

---

## 2. タスク着手時（承認ゲート — 必須）

**タスク単位で、着手前に以下を提示して止まる。承認を得るまでファイルを作成・編集しない。**（[09 §6](docs/09-implementation-tasks.md#L379-L409) の指示テンプレートと対応）

```
## 対象 / 対象外
- 実装するタスク（T-xx）と、明示的に対象外とするもの

## 参照した設計書
- 該当セクション

## 利用する既存基盤 / 新規作成するもの
- 既に存在するものを再実装していないことの確認（§3）

## 設計書の空白・矛盾（★ 重要）
- 確認が必要な事項 / 採用する仮定 / 設計書間の矛盾

## 実装計画
- 変更・新規作成ファイル、書くテスト、コミット単位

## 検証コマンドとベースライン
```

**「対象外」を必ず明示する。** 指定しないと関連する層まで一度に実装され、レビューが困難になる。

**設計書に書かれていない判断（境界値・並び順・既定値・NULL の扱い・エラー時の挙動）が必要になったら、推測で決めて進めない。** 既存コードに前例があればそれに従い（前例を根拠として報告）、無ければ確認事項として報告する。確認を待たず進める場合は**採用した仮定を必ず明示**する。

**設計書を無断で書き換えない。** 実装と設計書が食い違ったら、実装に合わせて設計書を直すのではなく報告する。設計書の不整合は [09 §6](docs/09-implementation-tasks.md) の通り実装前に指摘する。

---

## 3. 既存基盤を再実装しない

コードを書く前に、同等の機構が既にあるか確認する。このプロジェクトで**重複を作りやすい箇所**:

| 用途 | 置き場所 | やってはいけないこと |
|---|---|---|
| 例外 → HTTP ステータス変換 | `Api/Middleware/ExceptionHandlingMiddleware.cs`（[技術設計 §3.8](docs/08-technical-design.md)） | コントローラで個別に try-catch して独自の ProblemDetails を組み立てる |
| 例外の種別 | `Application/Common/`（NotFound / Conflict / DomainRule） | 新しい例外階層を作る |
| 現在時刻 | `IClock`（[技術設計 §3.1](docs/08-technical-design.md)） | `DateTime.Now` / `DateTime.UtcNow` を直接呼ぶ |
| JST 変換 | `Domain/Time/JstCalendar.cs` | 各所で個別に TimeZoneInfo 変換を書く |
| 分析の母集団定義 | `v_completed_sessions` ビュー（[DB設計 §4.1](docs/06-database-design.md)） | 各クエリに `status = 'Completed'` を直書きする |
| 列挙値 → 日本語表示名 | `frontend/src/lib/labels.ts`（[用語集 §0](docs/02-glossary.md)） | 画面ごとに日本語をハードコードする |
| UTC → JST 表示変換 | `frontend/src/lib/datetime.ts` | 画面ごとに変換を書く |
| API 呼び出し・ProblemDetails 解釈 | `frontend/src/api/client.ts` + `api/hooks/` | 画面から直接 fetch する |

**既存の書き方に合わせる。** 命名・ディレクトリ配置・非同期の扱い・テストの書き方は、同種の既存ファイルに合わせる。「一般的にはこう書く」を理由に既存様式から外れない。

---

## 4. ライブラリを追加しない

[技術設計 §0 / §1.1 / §1.2](docs/08-technical-design.md) で**明示的に不採用**としたもの。AI が「.NET / React の定番構成」として反射的に導入しやすいため、特に注意する。

**バックエンド**: MediatR / CQRS、AutoMapper、FluentValidation、リポジトリの汎用基底クラス、モックライブラリ（Moq 等）、キャッシュ（Redis 等）、認証基盤
**フロントエンド**: UIライブラリ、状態管理ライブラリ（Redux / Zustand / Jotai 等）、OpenAPI からの型自動生成

不採用の理由は各表の「理由」列にある。**必要だと判断した場合は、追加せず理由を報告する。**

---

## 5. アーキテクチャ上の絶対条件

- **Domain は何にも依存しない。** 外部パッケージも参照しない。EF Core の属性・`DbContext`・ASP.NET Core の型を Domain に持ち込まない。マッピングは Infrastructure の `IEntityTypeConfiguration` で行う（[技術設計 §2.1](docs/08-technical-design.md)）。
- **依存方向**: `Api → Application → Domain`、`Infrastructure → Domain, Application`。
- **サービスクラスは集約ごとに1つ。** ユースケースごとにクラスを作らない（[技術設計 §2.3](docs/08-technical-design.md)）。
- **1つの業務操作の複数書き込みは同一トランザクション**（start は WorkSession + PreWorkState + WorkContext、finish は WorkSession 更新 + PerformanceResult 挿入）。
- **同時実行はアプリケーション層のチェックだけで担保しない。** DB の部分一意インデックス `uq_work_sessions_single_active` が最終的な担保である（[技術設計 §3.6](docs/08-technical-design.md)）。
- **集約は常に全体を読む。** WorkSession は `PreWorkState` / `WorkContext` / `Result` を `Include` する。読み取り専用の一覧・分析は集約を経由せず射影・生SQL（[技術設計 §3.5 / §3.7](docs/08-technical-design.md)）。
- **エラーを握り潰さない。** 空の catch、既定値で失敗を覆い隠す処理を書かない。CHECK 制約違反（23514）は 400 ではなく 500 として扱う（到達した時点でバグであるため）。
- **N+1 を作らない。** ループ内・算出プロパティ内でのクエリ発行、`Include` 漏れを書いた時点で確認する。

---

## 6. 禁止事項（[技術設計 §8](docs/08-technical-design.md) の13項目）

実装中に気づいたら止める。全文は設計書にある。**特に破られやすい5つ**:

| # | 禁止事項 | 破られ方 |
|---|---|---|
| 11 | 作業前の入力項目を増やして30秒を超える | 「この項目も記録すれば後で分析できる」で増える。**最も破られやすい** |
| 2 | TaskItem に完了フラグ・期限・優先度・タグ・親子関係を追加する | 一般的なタスク管理アプリの機能を「あって当然」と判断して追加される |
| 7 | 分析結果に結論文（「午前が最も集中できています」等）を出す | 親切心で追加される |
| 1 | PreWorkState / WorkContext に setter や更新メソッドを追加する | EF Core のために public setter を付けた瞬間に失われる |
| 8 | サンプル数5件未満の平均値を返す・表示する | `HAVING COUNT(*) >= 5` と書きたくなる。**SQL でフィルタせず**件数ごと返して Application 層で判定する |

他に: 合成スコアの計算・表示（#5）、相関係数・p値（#6）、開始/終了時刻をリクエストで受け取る（#3）、成果評価のスキップ導線（#4）、記録の削除機能（#10）、`started_at::date` のような JST 変換なしの日付取り出し（#12）、設計書にない分析軸（#13）、欠損値の推測補完（#9）。

---

## 7. テスト

- **実装と同時に書く。カバレッジのためのテストを書かない。**[技術設計 §6.4](docs/08-technical-design.md) に「書かないテスト」がある（コントローラの単体テスト・DTOマッピングの網羅・E2E・カバレッジ目標）。
- **必ず書くテスト**は [技術設計 §6.2 / §6.3](docs/08-technical-design.md) の T-01〜T-14 / T-20〜T-25。省略しない。特に:
  - **T-02**（並行 start → 一方が409）: DB 制約の実効性を確認する唯一のテスト
  - **T-25**（C# の `JstCalendar.ToTimeBand` と SQL の `time_band` が24時間分一致）: JST 変換ロジックが2箇所にあるため、片方だけ変更されると分析結果がずれる
  - **T-12**（PreWorkState に public setter が無いことをリフレクションで確認）: 将来の変更を防ぐためのテスト
- **モックライブラリを使わない。** リポジトリのインメモリ実装を書く。
- **テストを通すためにテストを緩めない**（期待値を実測値に書き換える・assertion を消す・skip する）。落ちたら実装とテストのどちらが誤っているかを判断してから直す。判断がつかなければ報告する。

---

## 8. 完了報告

**未検証のまま「実装しました」と報告しない。** 以下をすべて満たしたときのみ完了とする。

1. ビルド・テストを実行し、**着手前に成功していた項目を壊していない**
2. 今回追加したテストが通っている
3. 依頼範囲の全項目を実装している（未実装があれば何を・なぜかを明示）
4. 動作未確認の箇所を明示している

報告に含めるもの:

- 変更・新規作成ファイルと主な変更内容
- **設計判断**（責務の置き場所とその根拠、共通化した/しなかった判断理由）
- **採用した仮定**（設計書に根拠が無く判断した箇所。無ければ「なし」）
- 検証結果（未実施は理由を明記）
- **積み残し**（依頼範囲外として実装しなかったもの、見つけた既存コードの問題、設計書の更新が必要な箇所）

### 報告前の自己レビュー（`git diff` に対して実施）

1. 既存の共通機構（§3）と同等のものを新規作成していないか
2. 依頼範囲外の機能・エンドポイント・画面・設定を追加していないか（§6 の禁止事項に該当していないか）
3. 設計書に根拠が無い判断を、確認せずに実装していないか
4. Domain が外部に依存していないか。責務の置き場所が §5 に沿っているか
5. エラーを握り潰していないか。`ExceptionHandlingMiddleware` を迂回していないか
6. N+1・`Include` 漏れ・トランザクション境界の欠落が無いか
7. デバッグ出力・コメントアウトしたコード・未使用 using / import・仮のハードコードが残っていないか
8. 接続文字列・実接続先が差分に含まれていないか
9. 命名が [用語集](docs/02-glossary.md) と揺れていないか（同じ概念に別の語を使っていないか）
10. `DateTime.Now` の直呼び・JST 変換なしの日付取り出しが無いか

---

## 9. 検証コマンド

リポジトリ初期化（T-00 / T-01）後に確定する。想定:

```bash
# バックエンド
dotnet build backend/PerformanceOs.sln
dotnet test  backend/PerformanceOs.sln          # 統合テストは Docker が必要

# フロントエンド
cd frontend && npm run build && npm run lint && npm run test

# DB（統合テストの前提）
docker compose up -d db
```

**マイグレーションは自動適用しない。**明示的に実行する（[技術設計 §4](docs/08-technical-design.md)）。

```bash
dotnet ef database update --project backend/src/PerformanceOs.Infrastructure \
                          --startup-project backend/src/PerformanceOs.Infrastructure
```

`Microsoft.EntityFrameworkCore.Design` は Infrastructure 側にあるため、startup も Infrastructure を指す。Api を startup にすると "doesn't reference Microsoft.EntityFrameworkCore.Design" で失敗する。設計時の DbContext は `PerformanceOsDbContextFactory` が組み立てる。

**副作用のあるコマンド**（`docker compose down -v`・`database drop`・実データを含む DB への操作）を確認なしに実行しない。

---

## 10. Git

- `main` + 作業ブランチ。縦切り単位でマージ（[技術設計 §7](docs/08-technical-design.md)）。
- **1関心事 = 1コミット。**各変更を単独で revert 可能な粒度にする。
- 着手前に未コミット変更・現在のブランチを確認し、既存作業を破壊する操作をしない。
- コミット・プッシュは指示されたときのみ行う。
